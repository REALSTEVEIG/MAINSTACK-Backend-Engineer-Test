import { AZURE } from './index';

const azureStorageConfig = {
  ACCOUNT: AZURE.STORAGE.ACCOUNT,
  ACCOUNT_KEY: AZURE.STORAGE.ACCOUNT_KEY,
  CONTAINER_NAME: AZURE.STORAGE.CONTAINER_NAME,
};
export default azureStorageConfig;
